// Entrego una permutacion y me devuelve la siguiente o la anterior.
// Cuando ya no hay mas, devuelve false.
// Complejidad por llamado O(n)
next_permutation(all(v)); // Si quiero verlas todas, ordenar ascendente
prev_permutation(all(v)); // Si quiero verlas todas, ordenar descendente
